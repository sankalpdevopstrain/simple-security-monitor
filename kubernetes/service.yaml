apiVersion: v1
kind: Service
metadata:
  name: simple-siem-service
spec:
  type: NodePort
  selector:
    app: simple-siem
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007
